#include <bits/stdc++.h>

using namespace std;

long long i = 1, n;
double mg = 0, a;

int main()
{
    cin >> n;
    
    while(i <= n)
    {
        cin >> a;
        mg = mg + a * a;
        i++;
    }
    
    cout << fixed << setprecision(2) << sqrt(mg / n);

    return 0;
}
